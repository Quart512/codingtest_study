---
tags: [오답노트, 프로그래머스, Lv1]
플랫폼: 프로그래머스
문제번호: 178871
난이도: Lv.1
날짜: 2026-09-09
결과: 시간 초과 → 해결
---

# 달리기 경주

- 문제: [프로그래머스 178871 - 달리기 경주](https://school.programmers.co.kr/learn/courses/30/lessons/178871)
- **코드: [코드/달리기경주.py](../코드/달리기경주.py)** ← 통과시킨 코드 원본

> [!info] 이 노트의 역할
> `.py` 에는 실제로 통과시킨 코드만 담백하게 둔다 (테스트 코드의 주석은 예외).
> 설명, 주석, 대안 풀이는 전부 이 노트에 적는다. 코드 파일은 기록, 노트는 회고.

## 무엇을 틀렸나

### 1. 시간 초과 (테스트 9~13)

```python
def solution(players, callings):
    answer = []
    for calling in callings:
        called = players.index(calling)
        players[called-1], players[called] = players[called], players[called-1]
    answer = players
    return answer
```

로직은 맞다. 테스트 16개 중 11개가 통과했고, 틀린 답을 낸 케이스는 하나도 없다.
**정확성이 아니라 속도에서 떨어졌다.**

**채점 결과**

| 테스트 | 결과 | 시간 |
|---|---|---|
| 1~4 | 통과 | 0.01 ~ 0.38ms |
| 5 | 통과 | 8.39ms |
| 6 | 통과 | 32.26ms |
| 7 | 통과 | 803.72ms |
| 8 | 통과 | 3186.95ms |
| 9~13 | **실패** | 시간 초과 |
| 14~16 | 통과 | 0.00ms |

**관찰**

- 6 → 7 에서 약 25배, 7 → 8 에서 약 4배. 입력이 커질수록 시간이 **입력 증가폭보다 훨씬 가파르게** 늘어난다
- 8번이 3.2초. 9번부터는 그 위 규모이므로 제한 시간을 넘긴 것으로 보인다
- 14~16 은 0.00ms — 작은 입력이라 문제 없음
- 즉 **알고리즘을 바꿔야 하는 문제**이지, 코드를 조금 다듬어서 될 문제가 아니다

**왜 그렇게 생각했나**

<!-- 이 방식으로 짤 때 머릿속에 있던 전제를 적을 것 -->

**해결**

`index()` 를 딕셔너리 조회로 바꿔서 **이름 → 인덱스**를 O(1)로 찾는다.

단, 이것만으로는 끝나지 않았다. 딕셔너리로 갈아탄 뒤 방향 실수(2번)와 반쪽짜리 해시(3번)를 거쳐서야 통과했다.
최종 해결 코드는 **3번의 해결**.

→ 반복문 안의 `.index()` 는 숨은 반복문이다. 입력 제한을 넣어 곱해 보고(5만 × 100만 = 500억) 안 되면 조회 방식을 바꾼다. [[시간복잡도]]

### 2. KeyError: None — 스왑 방향이 반대

`index()` 가 느린 걸 해결하려고 딕셔너리로 갈아탔다.

```python
def solution(players, callings):
    answer = []

    players_dict = {player: i for i, player in enumerate(players)}
    for calling in callings:
        key = next((k for k, v in players_dict.items() if v == players_dict[calling]+1), None)
        players_dict[calling] = players_dict[calling]+1
        players_dict[key] = players_dict[key]-1

    answer = players_dict
    return answer
```

`KeyError: None` 이 떴다. 그런데 **이건 증상이지 원인이 아니다.**

**원인**

딕셔너리의 값은 **인덱스**다. 인덱스 0 = 1등.
그러니까 **추월 = 인덱스가 줄어드는 것**이다.

그런데 `players_dict[calling]+1` 을 찾고 있었다.
이건 뒤에 있는 선수를 찾아서 `calling` 을 한 칸 **뒤로** 보내는 코드다.
추월이 아니라 **추월당하는** 로직을 짠 것.

`print` 를 찍어서 한 스텝씩 따라가 보면 바로 보인다:

```
before: {'mumu':0, 'soe':1, 'poe':2, 'kai':3, 'mine':4}  calling: kai
  찾은 key: mine            → kai 를 4번(꼴찌)으로 보냄
before: {'mumu':0, 'soe':1, 'poe':2, 'kai':4, 'mine':3}  calling: kai
  찾은 key: None            → 인덱스 5인 사람은 없음
KeyError: None
```

`kai` 가 꼴찌까지 밀려난 뒤 또 불리니 `v == 5` 인 선수가 없고,
`next(..., None)` 이 기본값 `None` 을 돌려줘서 `players_dict[None]` 에서 터졌다.

**왜 그렇게 생각했나**

"추월했다 = 순위 숫자가 올라간다" 로 읽고 `+1` 을 썼다.
하지만 딕셔너리에 담은 건 **순위가 아니라 인덱스**였다.
1등이 1이 아니라 0인 자료구조에서, 앞으로 가는 건 `-1` 이다.
**내가 저장한 값이 무엇인지**를 확인하지 않고 의미를 짐작한 게 원인.

**해결**

방향만 뒤집으면 정확성은 통과한다.

```python
key = next((k for k, v in players_dict.items() if v == players_dict[calling]-1), None)
players_dict[calling] -= 1
players_dict[key] += 1
```

1등은 추월할 대상이 없어서 `callings` 에 등장하지 않는다.
그래서 `-1` 로 고치면 `None` 이 나올 상황 자체가 사라진다.

### 3. 고쳐도 여전히 시간 초과 — dict 을 반쪽만 썼다

2번을 `-1` 로 고쳐도 제출하면 또 시간 초과다.

```python
key = next((k for k, v in players_dict.items() if v == players_dict[calling]-1), None)
```

이 한 줄이 **매번 딕셔너리 전체를 훑는다.** O(n)이다.
`index()` 를 없애려고 딕셔너리를 썼는데, 그 자리에 똑같이 O(n)짜리 전체 탐색을 다시 넣은 셈.
선수 5만 명 × 부름 100만 번 = O(n×m). 1번과 복잡도가 같다.

**원인**

딕셔너리로 만든 건 `이름 → 인덱스` 방향 하나뿐이다.
그런데 이 문제는 **`인덱스 → 이름`** 도 필요하다. 앞 선수가 누구인지 알아야 하니까.
없는 방향을 전체 탐색으로 때우는 순간 해시를 쓴 의미가 사라진다.

**해결**

`인덱스 → 이름` 은 `players` 리스트가 이미 갖고 있다. 새로 만들 필요가 없다.
리스트와 딕셔너리를 **항상 짝으로 갱신**하면 된다.

```python
def solution(players, callings):
    rank = {p: i for i, p in enumerate(players)}   # 이름 -> 인덱스
    for c in callings:
        i = rank[c]
        front = players[i-1]                       # 앞 선수: O(1)
        players[i-1], players[i] = players[i], players[i-1]
        rank[c], rank[front] = i-1, i              # 딕셔너리도 같이 갱신
    return players
```

부름 한 번당 전부 O(1). 전체 O(n+m).

그리고 2번 코드는 `answer = players_dict` 로 **딕셔너리를 반환**하고 있었다.
정답은 이름 리스트여야 한다. 시간 초과에 가려서 이 오류는 드러나지도 않았다.

## 이렇게 했으면 어땠을까

### 1. 만약 코드를 짜기 전에 "스왑에 무엇이 필요한가" 를 먼저 적었다면

부름 한 번을 처리하려면 두 가지를 알아야 한다.

- 불린 선수가 **몇 번째**인가 → 이름 → 인덱스
- 그 앞자리에 **누가** 있는가 → 인덱스 → 이름

두 방향 모두 O(1)이어야 한다는 게 처음부터 보였다면, dict 하나로 갈아탔다가 한 방향을 전체 스캔으로 때우는 3번 실수를 건너뛰었을 것이다.
**필요한 조회를 방향까지 적어 보는 것**만으로 자료구조가 정해진다.

### 2. 고려했지만 이 문제엔 과했던 방법: 역방향 dict 를 따로 만들기

```python
rank = {p: i for i, p in enumerate(players)}      # 이름 -> 인덱스
name_at = {i: p for i, p in enumerate(players)}   # 인덱스 -> 이름
```

동작은 한다. 하지만 **인덱스 → 이름은 `players` 리스트가 이미 하고 있다.**
dict 를 하나 더 만들면 같은 정보를 세 군데(`players`, `rank`, `name_at`)에서 함께 갱신해야 하고, 하나라도 빠뜨리면 조용히 틀린다.

| 방식 | 시간복잡도 | 부름마다 갱신할 곳 |
|---|---|---|
| `players` + `rank` (통과한 코드) | O(n + m) | 2곳 |
| `players` + `rank` + `name_at` | O(n + m) | 3곳 |

## 배운 것

1. dict 로 바꾼 뒤에도 반복문 안에서 `.items()` 를 훑고 있으면 해시를 반쪽만 쓴 것. 병목은 사라진 게 아니라 **반대 방향으로 옮겨갔을 뿐**이다
2. 조회가 양방향이면 **list(위치 → 값) + dict(값 → 위치)** 를 짝으로 두고 항상 같이 갱신한다. [[값으로 빠르게 찾아야 할 때]]
3. 인덱스를 저장한 dict 에서 "앞으로" 는 `-1`. **저장한 게 순위인지 인덱스인지** 먼저 확인한다
4. 풀기 전에 입력 제한으로 곱해 본다. 5만 × 100만 = 500억이면 코드를 짜기 전에 버릴 수 있었다

## 연결된 노트

- 개념: [[값으로 빠르게 찾아야 할 때]]
- 교과서: [[해시]] · [[시간복잡도]]
